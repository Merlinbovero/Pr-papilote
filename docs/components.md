# Catalogue des composants partagés

Composants de `src/components/shared/` et `src/components/layout/`. Les primitives `src/components/ui/` (shadcn) ne sont pas listées ici — voir [ui.shadcn.com](https://ui.shadcn.com).

Règle : tout nouveau composant partagé ajoute son entrée ici, dans le même commit.

| Composant                                                                  | Rôle | Props principales |
| -------------------------------------------------------------------------- | ---- | ----------------- |
| _(aucun pour l'instant — le catalogue se remplit au fil du développement)_ |      |                   |

## Modèle d'entrée

### `NomDuComposant`

Rôle en une phrase.

```tsx
<NomDuComposant prop="valeur" />
```
